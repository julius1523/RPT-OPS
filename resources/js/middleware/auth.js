// import Cookies from "js-cookie";

// export function initializeAuthentication(store) {
//     const token = Cookies.get("token");

//     if (token) {
//         // If a token is present, set the authentication state
//         store.commit("auth/SET_TOKEN", token);
//     }
// }
